# Railway Nixpacks configuration
# This file configures the build environment for Railway

[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg-full", "yt-dlp"]

[phases.install]
cmds = ["npm ci || npm install"]

[start]
cmd = "node server.js"
